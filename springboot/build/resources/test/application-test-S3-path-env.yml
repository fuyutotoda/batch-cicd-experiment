spring:
  profiles:
    group:
      test-S3-path-env: test-S3-path-env
  cloud:
    aws:
      region:
        instance-profile: false
        static: ap-northeast-1
  application:
    name: shinsoubatch
  datasource:
    url: jdbc:postgresql://localhost:5432/ssk?currentSchema=local
    username: admin
    password: CHANGEME
    driver-class-name: org.postgresql.Driver
  batch:
    jdbc:
      initialize-schema: never
    job:
      enabled: false

mybatis:
  mapper-locations: classpath:mapper/*.xml
s3:
  batch-s3-bucket: ''
  file-root-path: ''
  access-key: root
  secret: password
  endpoint: ''
logging:
  level:
    root: INFO
  file:
    name: logs/app.log

